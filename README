=======
Railgen
=======
This is a script for generating an HTML listing of a rail network in a
Minecraft world. How to use it:

1. Install Haml with ``gem install haml``.
2. Add your rail data to a ``.yaml`` file. See the ``reddit-pve-6-rails.yaml`` file
   for an example.
3. Run ``ruby railgen.rb -h`` for usage instructions.
